{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd public && cd ../server  && node . && npm run build",
    "build": "cd public && npm install && npm run build && cd ../server && npm install && node ."
  },
  "author": "",
  "license": "ISC"
}